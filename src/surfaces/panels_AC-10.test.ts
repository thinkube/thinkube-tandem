/**
 * Intentionally empty.
 *
 * The entry point's notification wiring is driven inside panels_AC-9, beside
 * the rule it is the call site for: the key the entry point passes and the
 * routing that key receives are one claim, and splitting them across two
 * files let each pass while the join between them went unexercised.
 *
 * This file is kept as a placeholder so the `_AC-10` name is not reused for
 * an unrelated claim.
 */
export {};
